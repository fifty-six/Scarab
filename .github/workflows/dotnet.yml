name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet publish -r win-x64 -p:PublishSingleFile=true --self-contained true
    - name: Upload Binary
      uses: actions/upload-artifact@v2
      with:
        name: windows-build
        path: Modinstaller2/bin/Modinstaller2/Release/netcoreapp3.1/win-x64/publish
